"""
HTTP API (FastAPI) — a thin layer over the async job system.

The app is `esports_poster_ai.api.app:app`. Run it with:

    uvicorn esports_poster_ai.api.app:app --reload
    python -m esports_poster_ai.api
"""
